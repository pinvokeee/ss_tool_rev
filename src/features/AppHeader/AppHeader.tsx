import { AppBar } from "@mui/material";
import Toolbar from "@mui/material/Toolbar";


export const AppHeader = () => {

    return (
    <AppBar>
        <Toolbar>
            Test
        </Toolbar>
    </AppBar>);
}